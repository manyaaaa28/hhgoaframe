# Auto-populated by Vercel when you attach a Blob store to this project
# (Project -> Storage -> Create Database -> Blob). Only needed for the
# desktop "share via link" fallback -- direct mobile share doesn't need it.
BLOB_READ_WRITE_TOKEN=
